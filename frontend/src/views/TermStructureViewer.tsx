// Term Structure workspace: ATM vol / variance across expiries.
import PlaceholderCard from "../components/PlaceholderCard";

export default function TermStructureViewer() {
  return (
    <PlaceholderCard title="Term Structure Viewer">
      Term structure of volatility and total variance across expiries, with the
      event-dilated calendar view, calendar-arbitrage diagnostics, and toggles
      between vol and variance space.
    </PlaceholderCard>
  );
}
