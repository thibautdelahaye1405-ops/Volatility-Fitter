// Graph workspace: smile-node graph for cross-asset signal propagation.
import PlaceholderCard from "../components/PlaceholderCard";

export default function GraphViewer() {
  return (
    <PlaceholderCard title="Graph Viewer">
      Graph of smile nodes (underlying, expiry) for propagating sparse
      observations to the full universe: edge-weight inputs, lit/dark node
      selection, interactive visualization, and solver controls.
    </PlaceholderCard>
  );
}
