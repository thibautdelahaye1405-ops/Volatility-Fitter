# Vol-Fitter Technical Notes — Roadmap

A series of standalone LaTeX technical papers documenting the mathematics,
algorithms, assumptions, heuristics, hyperparameters (surfaced **and** hidden),
numerical optimizations and performance of the Vol-Fitter application.

## Decisions of record (2026-06-28, confirmed with the user)

1. **Rewrite comprehensive.** Each topic that already had a theory-pure `.tex`
   note (LQD, Local-Vol, Multi-Core SIV, graph, weights) is *superseded* by a
   fuller note that fuses theory + implementation + performance. The legacy
   `.tex` files in `Docs/` are kept as historical sources but are no longer the
   reference; the `Docs/notes/NN_*.tex` files are.
2. **~15 focused topic notes**, one standalone document per topic.
3. **Clean-math body + appendices.** The body reads as rigorous mathematics at
   an advanced-undergraduate level. Every note ends with **Appendix A — a
   hyperparameter atlas** (each knob, surfaced or hidden, with its default and
   role) and **Appendix B — performance** (the numerical optimizations and
   measured timings). No inline `file:line` references in the body.
4. **Fresh figures/tables from the code.** Numerical examples, plots and timing
   tables are generated by running the actual fitter / benchmarks
   (`backend/.venv`), not reused. Generators live in `Docs/notes/figures/` and
   are re-runnable.

## Conventions

- Shared preamble: `Docs/notes/volfit_preamble.sty` (macros, theorem envs,
  `tcolorbox` asides: `heuristic` / `perfbox` / `caution` / `example`).
- Figures: `Docs/notes/figures/gen_<topic>.py` → `Docs/notes/figures/*.pdf`,
  each script importable and deterministic (fixed seeds, no network).
- Audience: advanced maths undergraduate; rigorous but an entertaining read;
  "leave nothing behind"; emphasize what is genuinely original.
- Build: `latexmk -pdf NN_topic.tex` (MiKTeX present).

## The 15 notes

| #  | File                              | Status vs legacy | Est. pp | Source material |
|----|-----------------------------------|------------------|---------|-----------------|
| 00 | `00_system_overview.tex`          | NEW              | 18–24   | CLAUDE.md, ROADMAP.md, whole `api/` + `calib/` |
| 01 | `01_lqd_model.tex`                | rewrite of `lqd_model_note.tex` | 30–40 | `models/lqd/*` |
| 02 | `02_svi_jw.tex`                   | NEW              | 16–20   | `models/svi_jw/*` |
| 03 | `03_multicore_siv.tex`            | rewrite of `Multi_Core_SIV_Technical_Note.tex` | 28–34 | `models/sigmoid/*` |
| 04 | `04_local_volatility.tex`         | rewrite of `piecewise_affine_..._calibration.tex` + methodology.md | 40–50 | `models/localvol/*` |
| 05 | `05_deamericanization.tex`        | NEW (from `deamericanization_..._note.md`) | 16–22 | `core/american*.py` |
| 06 | `06_forwards_dividends.tex`       | NEW              | 12–16   | forwards/parity/divs code |
| 07 | `07_calibration_objective.tex`    | NEW (absorbs `iv_time_value_density_weights.tex`) | 16–20 | `calib/{weights,band,rms}.py` |
| 08 | `08_variance_swaps.tex`           | NEW              | 12–16   | `calib/varswap.py`, `localvol/varswap_pde.py` |
| 09 | `09_wings_lee_bounds.tex`         | NEW (cross-model)| 12–16   | basis/lee across models |
| 10 | `10_calendar_arbitrage.tex`       | NEW              | 12–16   | `calib/calendar.py` |
| 11 | `11_event_variance_clock.tex`     | NEW              | 10–14   | `calib/{weighted_time,event_time}.py` |
| 12 | `12_spot_vol_dynamics.tex`        | rewrite of `spot_move_vol_surface_note_updated.tex` | 16–20 | `dynamics/{ssr,transport}.py` |
| 13 | `13_bayesian_prior_persistence.tex`| NEW (from prior-persistence design+roadmap) | 22–28 | `calib/{operators,factors,precision,prior}.py` |
| 14 | `14_graph_extrapolation.tex`      | rewrite of `ot_bayesian_graph_extrapolation_expanded.tex` + production binding | 40–55 | `graph/*`, `api/graph_*` |

Total ≈ 280–360 pp.

## Build order

1. **Infra** (preamble, figures harness, this roadmap). ← in progress
2. **01 LQD** — flagship template; establishes the house style + figure pipeline.
3. **00 System Overview** — the spine; references conventions set by 01.
4. Parametric cluster: **02 SVI-JW**, **03 Multi-Core SIV**, **09 Wings/Lee**.
5. **04 Local Volatility** (largest single note).
6. Quote-prep cluster: **05 De-Am**, **06 Forwards/Dividends**, **07 Objective**.
7. Targets/arbitrage: **08 Var-Swaps**, **10 Calendar**, **11 Event clock**.
8. Dynamics/priors: **12 Spot-Vol**, **13 Prior Persistence**.
9. **14 Graph Extrapolation** (capstone).

Each note: commit after it compiles clean with its figures.

## Post-series updates

- **2026-07-03 — editorial & mathematical hardening pass (pilot: Note 14).**
  House style codified in `STYLE_GUIDE.md` (Problem / Invariant / Mechanism /
  Implementation / Example opening contract, theorem-shaping, case files,
  figure rules, traceability tables). `volfit_preamble.sty` boxes softened to
  one quiet shape (left accent bar + faint wash) and a new `invariant` box
  added; all 15 PDFs rebuilt. Shared figure helper `figures/style.py` created
  (all future generators import it). **Note 14 rewritten as the pilot**:
  assumptions/definitions/propositions with proofs (PSD directed residual,
  proper prior, screened-Poisson OT dual, Schur-complement update), the
  six-node case file solved by the production code, regenerated
  narrative figures, exact LOO numbers (+37.1/+25.6 bp), and a traceability
  table with verified code/test anchors (Appendix C executed against
  `volfit.graph`, < 1e-10). The external comparison draft in
  `pilot_graph_extrapolation/` is superseded. Remaining notes get the same
  treatment in the priority order: 04, 13, 09+10, 02+03, 06/07/08/11/12,
  00 last, 01 lightly.
- **2026-07-03 — Note 04 hardened** (same pass, second note). Discrete theory
  made theorem-shaped: implicit-Euler step matrix proved an M-matrix with a
  discrete maximum principle / unconditional stability corollary, CN
  non-monotonicity remark (the measured Stage-7 arbitrage), discrete tangent
  AND adjoint propositions with the Lagrangian proof — noting production runs
  both (`LinearizedJacobian.apply_jacobian[_transpose]`), plus the R1 `.T`
  crash as a caution. Two case files (six-day weekly 108→23.5 bp; convex wing
  25.7→2.6 bp) and a new rescue-diagram figure generated by the production
  grid builder (`_delta_strike_nodes` / `_augment_per_expiry_coverage`,
  weekly 3→8 in-range vertices). Fixed vs code: production variance box
  (varLo 0.0025 / adaptive cap ≤400%), static var-swap replication integrates
  over the PDE grid (no `VS_HALF_WIDTH` there), Delaunay is the golden
  interp, `frontTie=True`, `_DELTA_SET`/`_CONVEX_WING_DELTA` in the atlas.
  Traceability table with exact test anchors; Appendix C verified against
  `models/localvol/dupire.py` (exact). Next: Note 13.
- **2026-07-03 — Note 13 hardened** (third note of the pass). The two gating
  mechanisms are now honestly distinguished (the operator/factor
  `activation_gap` precision-ratio gate vs the strike-gap coverage-deficit
  weighting — earlier revisions blurred them), with three propositions: the
  no-damp guarantee (closed gate ⇒ row identically zero), harmonic basket
  support = propagated leg precision, and the basket null-space theorem
  (RR/BF baskets are invariant under the common level shift that per-leg
  anchoring damps — the theorem behind `test_prior_nodamp.py`). Scoped the
  full precision vocabulary to its real consumer (the graph baseline);
  diagnostics endpoint documented as a field table; two case files (the
  4-vol-pt jump the strike anchor damped; the temporal backtest that
  confirmed hybrid, ~32 bp / 66%); new `fig_prior_activation` rendering both
  mechanisms from the production builders (ATM gap 0, RR25 0.99, BF25 0.95,
  VS 1.0; strike-gap unmet 67%). Atlas gains `collarSign` and
  `priorOperatorCovarianceMode` (diagonal-only implemented). Next: 09+10
  (wings/calendar arc).
- **2026-07-03 — Notes 09+10 hardened as one arc** (fourth+fifth of the pass).
  Note 10: Assumption block (fixed-forward normalization, deterministic
  carry, event-clock remark), the four-faces theorem (calls ⇔ convex order ⇔
  integrated quantiles ⇔ total variance) with proof sketches, Kellerer
  peacock remark, G=A(z) as a proposition; phantom-calendar promoted to a
  case file with a NEW `fig_cal_phantom` reproduced via the production
  `calibrate_svi` + `variance_floor_grid[_from]` (wide grid: far fit misses
  its own quotes by 151 vol bp; confined: 0.0); fixed the false claim that
  the floor threads to the LV overlay (LV is calendar-clean via ν≥0).
  Note 09: ψ-map lemma with proof, SIV zero-wing inheritance proposition,
  full R6 section (±2z 49-pt put-weighted Durrleman hinge, hybrid Jacobian,
  2-core cap; −10.2→−0.008, EEM ~400×, +79 bp) and the R3×R6 ablation case
  file (92/25/749/225 bp); **the confinement principle** named and boxed
  with its four instances and the two-curve/one-curve criterion.
  Traceability tables both notes; listings verified (lee_psi exact,
  lee_slopes 0 err). Next: 02+03.

- **2026-07-03 — content sync + reference code.** (1) Notes 03/05/09/00 were
  updated to reflect the shipped R3/R6 work: the Multi-Core SIV 2-core cap and
  put-wing Durrleman regularizer (`sivWingPenaltyPct`), the de-Americanization
  wing-convexity repair, and the R3×R6 ablation verdict (complementary, not
  redundant). (2) **Verified code snippets were added to all 15 notes** — an inline
  crux (≤15 lines) beside the central equation plus a fuller *Appendix C — Reference
  implementation* where warranted, ≤50 lines/note, numpy/scipy only. Each snippet is
  distilled from the production module and **executed** against it (agreement
  1e-10…1e-15); the standalone verifiers are session scratch, not committed. All PDFs
  rebuilt clean with `latexmk`.
